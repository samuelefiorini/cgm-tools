Install the `conda` environment for `cgm-tools` with
```sh
$ conda env create -f cgm-tools.yml
```
or
```sh
$ conda create --name cgm-tools --file spec-file.txt
```
then
```sh
$ pip install tensorflow-gpu
$ pip install keras
```
